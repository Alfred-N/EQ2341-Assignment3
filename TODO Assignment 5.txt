We need to:

* make a discriminator function that takes in an array of trained HMMs and a melody
and classifies it to a number in the range(len(HMMarray))

* make a training set and test set

* train and test
